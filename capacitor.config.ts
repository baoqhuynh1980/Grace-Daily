import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.faithdailywalk.gracedaily',
  appName: 'Grace Daily',
  webDir: 'build'
};

export default config;
